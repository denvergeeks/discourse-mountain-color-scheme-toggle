toggle_before_icon:
  default: "search"
  type: string
  description: "Insert the toggle before this header icon key"

show_for_anons:
  default: true
  type: bool
  description: "Show the mountain toggle for anonymous visitors too"

mountain_toggle_scale:
  default: 0.22
  type: float
  min: 0.1
  max: 1.0
  description: "Scale factor applied to the scene in the header"
